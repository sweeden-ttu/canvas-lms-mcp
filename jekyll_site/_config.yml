title: "CS5374 Project Proposal & Plan"
description: "Software Verification and Validation - Project proposal, plan, and timeline aligned with course syllabus"
baseurl: ""
url: ""

# Data directory points to root _data (universal, not just for Jekyll)
data_dir: "../_data"

markdown: kramdown
kramdown:
  math_engine: katex
  syntax_highlighter: rouge

exclude:
  - Gemfile
  - Gemfile.lock
  - vendor/
  - README.md

# plugins: jekyll-sitemap, jekyll-feed (add to Gemfile if needed)
