# Deep-dive: MCP server

Using the embedded tool docs for **mcp/{{SERVER_NAME}}**, list its tools, describe each tool's purpose and parameters, and give a step-by-step example using real endpoints/data where possible.

## Placeholders

- `{{SERVER_NAME}}`: MCP server name (directory under mcp/)
- `{{RELEVANT_CONTEXT}}`: filled from embedding retrieval for that server
